import React from 'react';
import styled from 'styled-components';

const SearchGridBox = styled('div')``;

const SearchContent = () => {
  return (
    <>
      <SearchGridBox></SearchGridBox>
    </>
  );
};

export default SearchContent;
